- primary_navigation = ['Home', 'Destinations', 'Shop', 'Hotels', 'Flights', 'Contact']
- secondary_navigation = ['Images', 'Video', 'Games']

%img.logo{src: "images/logo.png"}
%nav.nav.nav--primary
  %ul
    - primary_navigation.each do |item|
      %li
        %a{href: '#'}
          = item

%nav.nav.nav--secondary
  %ul
    - secondary_navigation.each do |item|
      %li
        %a{href: '#'}
          = item
